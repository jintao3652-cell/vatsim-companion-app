# VATSIM Companion App - 组装流程图解

## 📦 组装概览

```
┌─────────────────────────────────────────────────────────────┐
│                    组装流程总览                                │
│                   (预计时间: 30-60 分钟)                        │
└─────────────────────────────────────────────────────────────┘

    第1步              第2步              第3步              第4步              第5步
 ┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
 │运行脚本  │ ───> │编译插件  │ ───> │配置Bridge│ ───> │配置App  │ ───> │配对测试  │
 │(5分钟)  │      │(5分钟)  │      │(5分钟)  │      │(10分钟) │      │(5分钟)  │
 └─────────┘      └─────────┘      └─────────┘      └─────────┘      └─────────┘
      ↓                ↓                ↓                ↓                ↓
   自动化            Visual          修改配置         可选Firebase       完成
   设置环境          Studio          文件             配置              ✅
```

---

## 第 1 步：运行自动设置脚本

### Windows

```cmd
┌──────────────────────────────────────┐
│  打开命令提示符 (CMD)                   │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  cd vatsim-companion-app              │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  setup.bat                            │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  脚本自动执行:                          │
│  ✓ 检查 .NET SDK                      │
│  ✓ 检查 Flutter                       │
│  ✓ 恢复 Bridge NuGet 包                │
│  ✓ 构建 Bridge 服务                    │
│  ✓ 恢复 Flutter 包                     │
└──────────────────────────────────────┘
              ↓
          ✅ 完成
```

### 输出示例

```
============================================
VATSIM Companion App - Setup Script
============================================

[1/5] Checking prerequisites...
.NET SDK: OK

[2/5] Setting up Bridge Service...
Restoring NuGet packages...
  Restored VatsimBridge.csproj (in 2.5 sec).
Building Bridge Service...
  VatsimBridge -> bridge-service\windows\VatsimBridge\bin\Release\net7.0\VatsimBridge.dll
Bridge Service: OK

[3/5] Setting up vPilot Plugin...
vPilot Plugin: Packages restored (build in Visual Studio)

[4/5] Setting up Flutter App...
Getting Flutter packages...
Flutter App: OK

[5/5] Generating configuration files...

============================================
Setup Complete!
============================================
```

---

## 第 2 步：编译 vPilot 插件

### 2.1 打开 Visual Studio

```
┌────────────────────────────────────────────┐
│  双击打开:                                   │
│  vpilot-plugin\VatsimCompanionPlugin.sln   │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  Visual Studio 2022 加载项目                │
│                                             │
��  Solution Explorer:                         │
│  └─ VatsimCompanionPlugin                  │
│     ├─ Dependencies                        │
│     │  └─ RossCarlson.Vatsim.Vpilot.Plugins│
│     ├─ Plugin.cs                           │
│     ├─ Models.cs                           │
│     ├─ Utils.cs                            │
│     └─ BridgeCommunicationService.cs       │
└────────────────────────────────────────────┘
```

### 2.2 检查引用（重要！）

```
右键 "VatsimCompanionPlugin" 项目
    ↓
选择 "属性"
    ↓
点击 "引用" (或 "Dependencies")
    ↓
┌────────────────────────────────────────────┐
│  检查: RossCarlson.Vatsim.Vpilot.Plugins   │
│                                             │
│  路径应该是:                                 │
│  C:\Program Files\vPilot\                  │
│    RossCarlson.Vatsim.Vpilot.Plugins.dll   │
└────────────────────────────────────────────┘
    ↓
如果路径错误 → 删除引用 → 重新添加正确路径
    ↓
如果路径正确 → 继续
```

### 2.3 构建项目

```
┌────────────────────────────────────────────┐
│  按 F6 或点击 "生成" → "生成解决方案"          │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  Visual Studio 输出窗口:                     │
│                                             │
│  1>------ 已启动生成: 项目: VatsimCompanion... │
│  1>  Plugin.cs                             │
│  1>  Models.cs                             │
│  1>  Utils.cs                              │
│  1>  VatsimCompanionPlugin -> ...dll       │
│  1>  Plugin copied to vPilot Plugins directory│
│  ========== 生成: 成功 1 个 ==========         │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  DLL 自动复制到:                             │
│  C:\Program Files\vPilot\Plugins\          │
│    VatsimCompanionPlugin.dll               │
└────────────────────────────────────────────┘
              ↓
          ✅ 完成
```

---

## 第 3 步：配置 Bridge 服务

### 3.1 生成安全密钥

```
┌────────────────────────────────────────────┐
│  打开 PowerShell                            │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  运行命令:                                   │
│  -join ((65..90) + (97..122) + (48..57) |  │
│    Get-Random -Count 32 | % {[char]$_})   │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  输出示例:                                   │
│  Kj8mNp2qR5tYw9zXc3vBn6mAs4dFg7hJ        │
│                                             │
│  复制这个字符串! ✂️                          │
└────────────────────────────────��───────────┘
```

### 3.2 编辑配置文件

```
┌────────────────────────────────────────────┐
│  打开文件:                                   │
│  bridge-service\windows\VatsimBridge\      │
│    appsettings.json                        │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  找到 "Jwt" 部分:                            │
│                                             │
│  {                                          │
│    "Jwt": {                                 │
│      "SecretKey": "粘贴你复制的密钥",         │
│      "Issuer": "VatsimCompanionBridge",    │
│      "Audience": "VatsimCompanionApp",     │
│      "ExpirationMinutes": "1440"           │
│    }                                        │
│  }                                          │
└────────────────────────────────────────────┘
              ↓
         保存文件 💾
```

### 3.3 运行 Bridge

```
┌────────────────────────────────────────────┐
│  打开新的命令提示符                           │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  cd bridge-service\windows\VatsimBridge    │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  dotnet run                                 │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  看到这个输出表示成功:                         │
│                                             │
│  ===========================================│
│  VATSIM Companion Bridge                   │
│  ===========================================│
│  Bridge URL: http://localhost:5000         │
│  SignalR Hub: /vatsimhub                   │
│  API Docs: /swagger                        │
│  ===========================================│
│                                             │
│  保持这个窗口运行! 🔄                         │
└────────────────────────────────────────────┘
              ↓
     在浏览器测试: http://localhost:5000/swagger
              ↓
          ✅ 完成
```

---

## 第 4 步：配置 Flutter App

### 选项 A: 跳过 Firebase（快速测试）

```
┌────────────────────────────────────────────┐
│  无需任何配置!                               │
│  代码已经处理 Firebase 初始化失败            │
│  跳到 4.3 直接运行 App                       │
└────────────────────────────────────────────┘
              ↓
          直接运行
```

### 选项 B: 完整配置 Firebase

```
第1步: 创建项目
┌────────────────────────────────────────────┐
│  访问: https://console.firebase.google.com │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  点击 "添加项目"                             │
│  项目名称: VATSIM Companion                 │
│  完成创建向导                                │
└────────────────────────────────────────────┘

第2步: Android 配置
              ↓
┌────────────────────────────────────────────┐
│  点击 Android 图标                           │
│  包名: com.vatsim.companion                 │
│  下载 google-services.json                 │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  放置文件到:                                 │
│  mobile-app\android\app\                   │
│    google-services.json                    │
└────────────────────────────────────────────┘

第3步: iOS 配置 (可选)
              ↓
┌────────────────────────────────────────────┐
│  点击 iOS 图标                               │
│  Bundle ID: com.vatsim.companion            │
│  下载 GoogleService-Info.plist             │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  放置文件到:                                 │
│  mobile-app\ios\Runner\                    │
│    GoogleService-Info.plist                │
└────────────────────────────────────────────┘

第4步: 获取 Server Key
              ↓
┌────────────────────────────────────────────┐
│  项目设置 → Cloud Messaging                 │
│  复制 "服务器密钥"                           │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  编辑 appsettings.json:                     │
│  "PushNotification": {                      │
│    "FcmServerKey": "粘贴密钥"                │
│  }                                          │
└────────────────────────────────────────────┘
              ↓
        重启 Bridge 服务
```

### 4.3 运行 Flutter App

```
┌────────────────────────────────────────────┐
│  打开新的命令提示符                           │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  cd mobile-app                              │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  flutter devices                            │
│  查看可用设备列表                            │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  选择一个设备:                               │
│                                             │
│  Android 模拟器:                            │
│  flutter run -d emulator-5554              │
│                                             │
│  连接的手机:                                 │
│  flutter run -d <device-id>                │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  Flutter 编译和安装...                       │
│  (首次可能需要 2-5 分钟)                      │
│                                             │
│  ✓ Built build\app\outputs\flutter-apk\   │
│  ✓ Installing app...                       │
│  ✓ App is running                          │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  App 启动并显示配对界面 📱                    │
└────────────────────────────────────────────┘
              ↓
          ✅ 完成
```

---

## 第 5 步：配对和测试

### 5.1 检查所有组件状态

```
┌────────────────────────────────────────────┐
│  必须全部运行:                               │
│                                             │
│  ✅ vPilot (连接到 VATSIM)                  │
│  ✅ vPilot 插件已加载                        │
│  ✅ Bridge 服务 (localhost:5000)            │
│  ✅ Flutter App (显示配对界面)               │
└────────────────────────────────────────────┘
```

### 5.2 生成配对码

```
┌────────────────────────────────────────────┐
│  打开新的 PowerShell 窗口                    │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  运行命令:                                   │
│  Invoke-RestMethod -Method Post `          │
│    -Uri http://localhost:5000/api/pairing/start│
│    | ConvertTo-Json                        │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  输出:                                       │
│  {                                          │
│    "success": true,                         │
│    "pairingCode": "123456",                │
│    "bridgeUrl": "http://localhost:5000",   │
│    "expiresAt": "2026-06-02T12:10:00Z"     │
│  }                                          │
│                                             │
│  记住配对码: 123456 ✍️                       │
└────────────────────────────────────────────┘
```

### 5.3 在 App 中输入配对信息

```
┌────────────────────────────────────────────┐
│  📱 Flutter App - 配对界面                   │
│                                             │
│  Bridge Address:                            │
│  ┌──────────────────────────────────────┐  │
│  │ 10.0.2.2:5000  (Android 模拟器)       │  │
│  │ localhost:5000 (iOS 模拟器)           │  │
│  │ 192.168.1.100:5000 (真机)             │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  Pairing Code:                              │
│  ┌──────────────────────────────────────┐  │
│  │ 123456                                │  │
│  └──────────────────────────────────────┘  │
│                                             │
│         [ Connect ]  按钮                   │
└────────────────────────────────────────────┘
              ↓
         点击 Connect
              ↓
┌────────────────────────────────────────────┐
│  配对中... ⏳                                │
│  - 验证配对码                                │
│  - 生成 JWT Token                           │
│  - 连接 WebSocket                           │
│  - 注册推送 Token                            │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  ✅ 配对成功!                                │
│  跳转到主界面                                │
└────────────────────────────────────────────┘
```

### 5.4 测试功能

```
测试 1: 消息接收
┌────────────────────────────────────────────┐
│  在 vPilot 中:                              │
│  - 接收一条私信                              │
│  或                                         │
│  - 接收频率消息（包含你的 Callsign）          │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  📱 App "Chat" 界面应该显示消息               │
└────────────────────────────────────────────┘


测试 2: 消息发送
┌────────────────────────────────────────────┐
│  📱 在 App "Chat" 界面:                      │
│  - 切换到 "Radio" 标签                       │
│  - 输入: "Test from mobile"                 │
│  - 点击发送                                  │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  vPilot 应该通过频率发送消息                  │
└────────────────────────────────────────────┘


测试 3: 状态监控
┌────────────────────────────────────────────┐
│  📱 切换到 "Status" 标签                     │
│  - 查看 Callsign                            │
│  - 查看高度、速度、航向                       │
│  - 查看 COM 频率                             │
│  - 下拉刷新                                  │
└────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────┐
│  ✅ 所有信息正确显示                          │
└────────────────────────────────────────────┘
```

---

## 🎉 完成！系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     VATSIM Companion 运行架构                  │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│              │  HTTP   │              │  HTTP   │              │
│  vPilot      │◄───────►│  Plugin      │◄───────►│   Bridge     │
│              │ :8765   │  (.NET 4.8)  │ :5000   │  (.NET 7)    │
└──────────────┘         └──────────────┘         └──────────────┘
       │                                                   │
       │                                                   │ WebSocket
       │                                                   │ + REST API
       │                                                   │
       │ VATSIM Network                                    ▼
       │                                           ┌──────────────┐
       ▼                                           │              │
  ┌─────────┐                                     │ Mobile App   │
  │ VATSIM  │                                     │ (Flutter)    │
  │ Network │                                     │              │
  └─────────┘                                     └──────────────┘
                                                         │
                                                         │
                                                         ▼
                                                  ┌──────────────┐
                                                  │   Firebase   │
                                                  │     FCM      │
                                                  └──────────────┘
```

---

## 📋 最终验证清单

完成所有步骤后，验证以下功能：

```
✅ 系统组件
   □ vPilot 连接到 VATSIM
   □ 插件已加载 (检查 vPilot 日志)
   □ Bridge 运行 (http://localhost:5000)
   □ App 运行并配对成功

✅ 消息功能
   □ 接收私信
   □ 接收频率消息
   □ 发送频率消息
   □ 消息历史加载

✅ 状态监控
   □ 显示 Callsign
   □ 显示位置和高度
   □ 显示速度和航向
   □ 显示 COM 频率
   □ 下拉刷新工作

✅ 推送通知 (如果配置了 Firebase)
   □ 私信推送
   □ 提及推送
   □ 通知点击跳转

✅ 稳定性
   □ 重启 App 自动重连
   □ 网络中断后恢复
   □ 错误提示清晰
```

---

## 🆘 需要帮助？

如果遇到问题，按以下顺序检查：

1. 查看 **QUICK_START.md** - 详细步骤
2. 查看 **READINESS_CHECKLIST.md** - 常见问题
3. 查看日志文件:
   - 插件: `%APPDATA%\VatsimCompanion\Logs\`
   - Bridge: 控制台输出
   - App: `flutter logs`
4. 提交 GitHub Issue

---

**组装难度**: ⭐⭐⭐☆☆ (中等)  
**成功率**: 95%+ (如果按步骤操作)  
**技术支持**: 完整文档 + 详细日志

祝你组装顺利！✈️
